{
  "name": "afc-staking-contracts",
  "version": "1.0.0",
  "type": "module",
  "description": "AFC Staking Smart Contracts for Scroll Network",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:local": "hardhat run scripts/deploy_staking.js --network hardhat",
    "deploy:scroll-sepolia": "hardhat run scripts/deploy_staking.js --network scrollSepolia",
    "deploy:scroll": "hardhat run scripts/deploy_staking.js --network scroll",
    "verify": "hardhat run scripts/verify.js"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "@nomicfoundation/hardhat-network-helpers": "^1.0.0",
    "@openzeppelin/contracts": "^5.0.0",
    "dotenv": "^16.0.0",
    "hardhat": "^2.22.0"
  }
}
